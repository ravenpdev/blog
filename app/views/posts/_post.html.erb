<div id="<%= dom_id post %>">
  <h2>
    <%= post.title %>
  </h2>

  <div>
    <%= post.body %>

    <div>
      <span class="badge bg-primary"><%= pluralize(post.views, "view") %></span>
    </div>
  </div>
</div>
